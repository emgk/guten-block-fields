<CheckboxControl
		heading="#checkbox-title#"
		label="#checkbox-label#"
		help="#checkbox-help#"
		checked={ #checkbox-isCheck# }
		onChange={ ( isChecked ) => {
			 // setState( { isChecked } ) 
		} }
/>
